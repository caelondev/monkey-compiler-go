fn ack(m, n) {
  if (m == 0) {
    return n + 1;
  }
  if (n == 0) {
    return ack(m - 1, 1);
  }
  return ack(m - 1, ack(m, n - 1));
}

var start = time();
var result = ack(3, 10);
var end = time();

print("Result: "+ to_string(result));
print("Time: "+ to_string(end - start) +" ms");
